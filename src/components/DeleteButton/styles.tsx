import { css } from "@stitches/react";

export const deleteIconImg = css({
  padding: "15px 14px 0 0",
  position: "absolute",
  right: 0,
  top: 0,
  cursor: "pointer",
  "&:hover": {
    transform: "scale(1.1)",
  },
});
